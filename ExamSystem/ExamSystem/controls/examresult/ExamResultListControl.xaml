<UserControl x:Class="ExamSystem.controls.examresult.ExamResultListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="562" d:DesignWidth="1280">
    <Grid>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="本轮处置答案对照" Foreground="#CC0000" FontSize="40" HorizontalAlignment="Center" />
            
            <ListBox IsSynchronizedWithCurrentItem="True" Name="qlist" SelectionMode="Single" Background="Transparent" BorderBrush="Transparent" Margin="0 30 0 0" HorizontalAlignment="Center">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="10" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
            <TextBlock Text="说明：" Foreground="Black" FontSize="25" Margin="0 20 0 0" />
            <TextBlock Text="1. 红色按钮为扣分题，双击题号进行答案对比。" Foreground="Black" FontSize="25" Margin="0 10 0 0" />
            <TextBlock Text="2. 查看后题号将变为蓝色。" Foreground="Black" FontSize="25" Margin="0 10 0 0" />
        </StackPanel>
    </Grid>
</UserControl>