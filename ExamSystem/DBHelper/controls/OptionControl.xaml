<UserControl x:Class="DBHelper.controls.OptionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel Orientation="Vertical" Margin="10">
            <DataGrid AutoGenerateColumns="False"
                      HorizontalAlignment="Center"
                      Name="dg_options"
                      ItemsSource="{Binding}"
                      Width="270">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="描述"
                                        Width="220"
                                        Binding="{Binding Path=Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    <DataGridCheckBoxColumn Header="正确"
                                            Width="50"
                                            Binding="{Binding Path=Correct, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
        
    </Grid>
</UserControl>
