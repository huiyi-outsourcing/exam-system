<Window x:Class="DataMigration.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="MainWindow" Height="600" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Name="create" Content="创建数据库" Click="create_Click" Width="100" Margin="20 0 20 0" />
            <Button Name="import" Content="导入试题" Click="import_Click" Width="100" Margin="20 0 20 0" />
            <!--
            <Button Name="test" Content="测试" Click="test_Click" Width="100" Margin="20 0 20 0"/>
            -->
        </StackPanel>
        
        <Grid Grid.Row="1">  
            <ListView Name="_listview" ItemsSource="{Binding FileCollection}" >
                <ListView.View>
                    <GridView>
                        <GridViewColumn Width="320" Header="文件" DisplayMemberBinding="{Binding FilePath}" />
                        <GridViewColumn Width="460" Header="状态" DisplayMemberBinding="{Binding FileStatus}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Grid>
</Window>
